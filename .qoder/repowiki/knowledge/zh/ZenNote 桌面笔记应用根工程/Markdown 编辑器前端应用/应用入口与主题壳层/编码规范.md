- 主题切换通过操作 document.documentElement.classList.toggle('dark', ...) 实现，而非内联 style。
- 全局设计令牌统一集中在 index.css 的 :root 中声明为 CSS 自定义属性（--text/--bg/--accent 等），组件样式通过 var() 引用。
- 响应式断点统一使用 @media (max-width: 1024px) 作为移动端适配阈值。
- 副作用（如 matchMedia 监听）在 useEffect 中注册并在返回的清理函数中移除，避免内存泄漏。
- PDF 导出窗口通过检测 window.__TAURI_INTERNALS__.metadata.currentWindow.label 前缀来跳过应用挂载。
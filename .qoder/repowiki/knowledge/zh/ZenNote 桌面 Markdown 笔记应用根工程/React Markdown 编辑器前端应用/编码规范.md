- 状态管理统一通过 Zustand 的单一 `useStore` hook 按 slice 访问，避免分散的 useState/useReducer。
- 组件按功能域划分到 `components/<domain>/` 子目录，布局组件集中在 `components/layout/`。
- 国际化文案通过 `i18n/index.ts` 的 `t()` 函数获取，语言包以 `zh-CN.ts` / `en-US.ts` 形式并列存放。
- 主题模式在 `App.tsx` 中集中解析并写入 DOM class，各组件仅消费 store 中的 resolvedMode。
- TypeScript 采用项目引用模式（references），通过 tsconfig.json 聚合 app 与 node 两个子配置
- 前端状态管理统一使用 Zustand store，由 AppShell 布局组件组装各功能面板
- 前后端通信通过 @tauri-apps/api 调用 Tauri 命令，而非 HTTP 接口
- 代码风格与规则由 .oxlintrc.json 统一管理，启用 react、typescript、oxc 插件